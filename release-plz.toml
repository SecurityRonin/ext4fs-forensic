# release-plz owns crates.io publishing + changelogs + version bumps for the
# workspace LIBRARY — the reader `ext4fs-core`. It runs on pushes to main (see
# .github/workflows/release-plz.yml):
#   - `release-pr`  opens a human-reviewed version-bump PR (conventional commits)
#   - `release`     publishes any crate whose Cargo.toml version is ahead of
#                   crates.io, in dependency order, and creates its git tag
#
# The `fuzz` crate is not a workspace member and is publish = false, so release-plz
# never sees it.
#
# NOTE: this workspace hoists a single shared version ([workspace.package].version),
# so all three members move together on a bump. Only `ext4fs-core` is a published
# library; the two binaries are `release = false` below, so release-plz bumps their
# manifest version in lockstep but never publishes them.

[workspace]
# Per-package git tags, e.g. `ext4fs-core-v0.2.4`. A `<name>-v…` tag has a letter,
# not a digit, after the first `v`, so it never matches a `v[0-9]*` binary-release
# trigger.
git_tag_enable = true
# Library crates get a git tag + CHANGELOG entry, not a separate GitHub Release.
git_release_enable = false
changelog_update = true
dependencies_update = false
# Only prepare a release when a real change exists since the last one. This
# allowlist makes feat/fix/perf/refactor/doc/revert the only release triggers;
# chore/ci/test/style/build (incl. release-plz's own release commits) never
# prepare a release (kills the changelog-churn loop).
release_commits = "^(feat|fix|perf|refactor|doc|revert)"

[changelog]
commit_parsers = [
    { message = "^chore\\(release\\):", skip = true },
    { message = "^chore: release", skip = true },
    { message = "^feat", group = "added" },
    { message = "^fix", group = "fixed" },
    { message = "^perf", group = "performance" },
    { message = "^refactor", group = "other" },
    { message = "^doc", group = "documentation" },
    { message = "^revert", group = "other" },
    { message = "^style", skip = true },
    { message = "^test", skip = true },
    { message = "^ci", skip = true },
    { message = "^build", skip = true },
    { message = "^chore", skip = true },
    { message = "^.*", group = "other" },
]

# The FUSE mount binary — a front-end, not a published library.
[[package]]
name = "ext4fs-fuse"
release = false

# The CLI (binary `ext4fs`) — a front-end, not a published library.
[[package]]
name = "ext4fs-cli"
release = false
